jar {
    baseName = 'statistics-repository'
}

dependencies {
    compile project(':acontrol-core')
    compile 'org.springframework.data:spring-data-redis:1.6.1.RELEASE'
    compile 'redis.clients:jedis:2.4.2'
}
